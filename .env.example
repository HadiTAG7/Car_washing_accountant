# ═══════════════════════════════════════════════════════════════════════════
# Firebase configuration for Sweater (سويتر)
# ═══════════════════════════════════════════════════════════════════════════
#
# Copy this file to ".env.local" and fill in the values from your Firebase
# project: Project Settings → General → Your apps → Web app → SDK config.
#
# These are PUBLIC client identifiers (not secrets) — access is enforced by
# Firestore/Storage security rules, not by hiding the key. On Vercel, set the
# same variables under Project → Settings → Environment Variables.
#
# If the required vars are missing, the app runs in DEMO MODE (UI preview
# with seed data, no live backend).

VITE_FIREBASE_API_KEY=YOUR-WEB-API-KEY
VITE_FIREBASE_AUTH_DOMAIN=YOUR-PROJECT.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=YOUR-PROJECT-ID
VITE_FIREBASE_STORAGE_BUCKET=YOUR-PROJECT.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR-SENDER-ID
VITE_FIREBASE_APP_ID=YOUR-APP-ID

# Optional — login is REQUIRED by default (secure default, matching the
# Firestore security rules that only allow authenticated access). Set to
# "false" only for local demos.
VITE_REQUIRE_AUTH=true

# Server-only Vercel Function configuration. Never prefix these with VITE_.
# AGENT_COMMAND_CENTER_FIREBASE_SERVICE_ACCOUNT is preferred. Existing Vercel
# projects may reuse the server-only FIREBASE_SERVICE_ACCOUNT variable instead.
AGENT_COMMAND_CENTER_FIREBASE_PROJECT_ID=gemini-eed4a
AGENT_COMMAND_CENTER_FIREBASE_SERVICE_ACCOUNT=
AGENT_COMMAND_CENTER_WEBHOOK_SECRET=
CRON_SECRET=
AGENT_COMMAND_CENTER_ENDPOINT=https://monster-wash-erp.vercel.app/api/agent-command-center-ingest

# ── روابط المساعد الذكي للشركاء (api/partner-mcp) ──
# FIREBASE_SERVICE_ACCOUNT: server-only, the full service-account JSON. Already
# required by api/ledger.js; the partner MCP endpoint answers 503 without it.
FIREBASE_SERVICE_ACCOUNT=
# Optional, PUBLIC: the origin shown in a partner's MCP link. Only needed when the
# app runs somewhere whose window.location.origin is not the Vercel host (the
# Capacitor APK). Falls back to VITE_LEDGER_API_URL's origin, then the page.
VITE_PARTNER_MCP_BASE_URL=
