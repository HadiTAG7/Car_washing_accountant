# تعريف `amount` على سجلات المشتريات

> سؤال واحد لا يحتمل إجابتين: **المبلغ المسجَّل على المصروف — هل هو شامل
> الضريبة أم قبلها؟**

الحقول `total_monthly_cost` و`total_variable_cost` و`amount` أسماء لا تقول
شيئاً عن هذا. وحين تُترك الإجابة للتخمين تختلف الشاشات: تعرض صفحة المصاريف
ضريبةً مستخرجة من المبلغ بوصفه إجمالياً، ويقرأ قيد الترحيل المبلغ نفسه بوصفه
صافياً، فيصير للرقم الواحد معنيان — والفرق بينهما هو الضريبة كلها.

## التعريف

`price_mode` هو ما يحسم، ولا شيء غيره:

| `price_mode` | معنى `amount` | إجمالي السداد | ما يأخذه حساب المصروف/الأصل |
|---|---|---|---|
| `inclusive` (الافتراضي) | **الإجمالي** شامل الضريبة | `amount` | `amount − VAT` |
| `exclusive` | **الصافي** قبل الضريبة | `amount + VAT` | `amount` |

بعبارة أخرى:

```
inclusive →  gross = amount            net = gross − VAT
exclusive →  net   = amount            gross = amount + VAT
```

وفي الحالتين: `net + VAT = gross`، والفرق الناتج عن التقريب يقع على `net`
لا على الضريبة — لأن الضريبة هي الرقم الذي يُقدَّم في الإقرار ويجب أن يطابق
المستند بالهللة.

### حين لا توجد ضريبة أصلاً

سجل ليس فاتورة ضريبية (`is_tax_invoice: false`) لا يُستشار فيه `price_mode`:
لا يوجد شقّان ليُقال أيهما المكتوب. المبلغ هو التكلفة كلها،
و`gross = net = amount`.

وكذلك الضريبة غير القابلة للخصم: تُحسب لمعرفة ما دُفع فعلاً (وهو ما يحدد
`gross` في وضع `exclusive`)، لكنها تبقى داخل التكلفة ولا يُفتح لها حساب
1200 — ضريبة لا تُستردّ جزءٌ من ثمن الشيء، لا ذمة على الهيئة.

## تاريخ الفاتورة وتاريخ الدفع

المبلغ سؤال، والتاريخ سؤال ثانٍ لا يقل عنه. فاتورة المورّد مؤرخة 31 مارس
وتُدفع 2 أبريل: ضريبة المدخلات تُطالَب في **فترة الفاتورة**، والنقد يخرج
**يوم الدفع**. وقيد واحد لا يحمل التاريخين.

كان القيد الواحد مؤرَّخاً بيوم الدفع، فوقع الحساب 1200 في أبريل بينما
يطالب التقرير بضريبته في مارس:

```
Q1:  تقرير 15 · دفاتر 0
Q2:  تقرير 0  · دفاتر 15
```

وهي مطابقة لا تُغلق أبداً. وتأريخ القيد كله بيوم الفاتورة يصحّح الضريبة
ويكسر النقد: الصندوق يتحرك في مارس والمال خرج في أبريل.

فالحل هو المحاسبة على أساس الاستحقاق، لا حيلة تأريخ — **قيدان**:

```
إثبات الفاتورة — بتاريخ الفاتورة (31 مارس):
    مدين   5100 مصروف          100.00
    مدين   1200 ضريبة مدخلات    15.00
    دائن   2000 الموردون                115.00

السداد — بتاريخ الدفع (2 أبريل):
    مدين   2000 الموردون       115.00
    دائن   1010 الصندوق                 115.00
```

ويُدمَج القيدان في واحد حين يقع التاريخان في اليوم نفسه — وهو الغالب — لأن
الذمة تنشأ وتُسدَّد في اللحظة ذاتها فلا تصف شيئاً. والمصروف غير المسدَّد
لا سداد له أصلاً: تبقى الذمة قائمة.

**الثابت المطلوب:** ضريبة التقرير لفترةٍ = حركة 1200 في الفترة نفسها، مع
بقاء تاريخ حركة النقد هو تاريخ الدفع الحقيقي.

قيد السداد يحمل `settlementOf` يشير إلى قيد الفاتورة، ولا يحمل
`purchaseTaxSnapshot` إطلاقاً — فلا يستطيع قارئٌ يجمع اللقطات أن يحتسب
الخصم مرتين. وعكس قيد الفاتورة يعكس معه قيد السداد في المعاملة نفسها،
وإلا بقي سدادٌ مقابل ذمةٍ لم تعد موجودة.

## من يطبّق هذا التعريف

كل الطبقات، من الدالة نفسها:

| الموضع | كيف |
|---|---|
| محرك الضريبة على الخادم | `functions/src/purchaseTax.js` → `resolvePurchaseTax` |
| توأمه على العميل | `src/lib/accounting/purchaseTax.js` (نفس البطارية في `functions/test/purchaseTax.test.js`) |
| قيد المصروف (إثبات + سداد) | `functions/src/posting.js` → `buildExpense` |
| كتابة القيدين ذرياً | `functions/src/ledger.js` → `postSource` |
| عكس القيدين معاً | `functions/src/ledger.js` → `reverseEntry` |
| معاينة القيد | `src/lib/accounting/postingRules.js` → `buildExpenseEntry` |
| تقرير ضريبة القيمة المضافة | `src/lib/accounting/vatReturn.js` → `purchaseEntryTax` للمُرحّل، و`inputInvoiceTax` (غلاف على المحرك) لغيره |
| العرض والمجاميع التشغيلية | `src/lib/accounting/purchaseDisplay.js` → `purchaseAmounts` |
| تصدير CSV | `src/components/VatRecoveryPage.jsx` (أعمدة مسمّاة: الإجمالي/الصافي/الضريبة) |

## قاعدة العرض

لا يُعرض رقم باسم «الإجمالي» أو «الصافي» دون أن يكون مصدره
`purchaseAmounts` أو `resolvePurchaseTax`. وإذا تعذّر تحديد الضريبة — لا
مبلغ على الفاتورة ولا نسبة مثبتة ولا سياسة تغطي تاريخها — تُعرض العبارة
«غير محدَّدة» ولا يُخترع رقم. الاختراع الوحيد الذي كان يحدث هو افتراض 15%،
وهو يعطي فاتورة من عصر الـ5% استرداداً ثلاثة أضعاف ما دُفع.

## لماذا الافتراضي `inclusive`

لأن كل ما سبق كُتب على هذا الأساس: `extractVat(total_monthly_cost)` كان
يستخرج الضريبة من المبلغ بوصفه إجمالياً، وهو أيضاً ما يعنيه محاسب سعودي
حين يقول «الإيجار 5,000». فالسجلات القديمة التي لا تحمل `price_mode`
تُقرأ `inclusive`، وهي القراءة نفسها التي كانت تُقرأ بها.
