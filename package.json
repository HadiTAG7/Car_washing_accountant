{
  "name": "car-washing-accountant",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/assert-production-source.mjs",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest run --exclude \"**/*.emulator.test.js\" --exclude \"work/**\"",
    "test:watch": "vitest",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-sweater --config firebase.test.json \"vitest run --testTimeout=120000 --no-file-parallelism --exclude work/** ledger.emulator invoicing.emulator assets.emulator recurring.emulator autoPost.emulator startupMigration.emulator\"",
    "test:rules": "firebase emulators:exec --only firestore --project demo-sweater --config firebase.test.json \"vitest run --testTimeout=120000 --exclude work/** rules.emulator\"",
    "test:functions": "firebase emulators:exec --only firestore --project demo-sweater --config firebase.test.json \"vitest run --testTimeout=120000 --no-file-parallelism --dir functions --exclude **/callables.test.js --exclude work/**\"",
    "test:callables": "firebase emulators:exec --only firestore,functions,auth --project demo-sweater --config firebase.test.json \"vitest run --testTimeout=180000 --no-file-parallelism --dir functions --exclude work/** callables httpApi\"",
    "verify": "npm run lint && npm run build && npm test && npm run test:emulator && npm run test:rules && npm run test:functions && npm run test:callables",
    "deploy": "npm run verify && npm run deploy:backend",
    "deploy:backend": "firebase deploy --only functions:ledger,firestore:rules",
    "deploy:functions": "firebase deploy --only functions:ledger",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "agents:send": "node scripts/send-agent-command-event.mjs",
    "bootstrap:admin": "node scripts/bootstrap-admin.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@modelcontextprotocol/sdk": "^1.20.0",
    "firebase": "^12.16.0",
    "firebase-admin": "^13.10.0",
    "html2pdf.js": "^0.14.0",
    "qrcode-generator": "^2.0.4",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@eslint/js": "^9.39.4",
    "@firebase/rules-unit-testing": "^5.0.1",
    "@tailwindcss/vite": "^4.2.2",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^9.39.4",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "firebase-tools": "^15.26.0",
    "globals": "^17.4.0",
    "jsdom": "^26.1.0",
    "lucide-react": "^1.8.0",
    "recharts": "^3.8.1",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.4",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
